<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <!--    <uses-permission android:name="com.google.android.gms.permission.ACTIVITY_RECOGNITION"/>-->

    <application
        android:name=".ElegantCommerce"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ElegantCommerce"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        <!-- Activity de inicio -->
        <activity
            android:name=".activity.LoginRegisterActivity"
            android:exported="true"
            android:theme="@style/Theme.ElegantCommerce">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- Shopping Activity -->
        <activity
            android:name=".activity.ShoppingActivity"
            android:exported="false" />
        <!-- Payment Successful (Deep Link) -->
        <activity
            android:name=".activity.CongratsActivity"
            android:exported="true"
            android:label="@string/deeplink">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <action android:name="android.intent.category.DEFAULT" />
                <action android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="elegantcommerce" android:host="congrats" />
            </intent-filter>
        </activity>
        <!-- Payment Failed (Deep Link) -->
        <activity
            android:name=".activity.FailureActivity"
            android:exported="true"
            android:label="@string/deeplink">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <action android:name="android.intent.category.DEFAULT" />
                <action android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="elegantcommerce" android:host="failure" />
            </intent-filter>
        </activity>
        <!-- Payment Pending (Deep Link) -->
        <activity
            android:name=".activity.PendingActivity"
            android:exported="true"
            android:label="@string/deeplink">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <action android:name="android.intent.category.DEFAULT" />
                <action android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="elegantcommerce" android:host="pending" />
            </intent-filter>
        </activity>
    </application>

</manifest>